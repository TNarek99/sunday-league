type User {
    id: ID!
    firebaseId: ID!
    firstName: String!
    lastName: String!  
}

extend type Query {
    users: [User!]
    currentUser: User!
}

extend type Mutation {
    updateUser: User!
}
