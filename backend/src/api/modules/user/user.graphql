type User {
    id: ID!
    firebaseId: ID!
    firstName: String
    lastName: String
    birthDate: DateTime
    gender: Gender
    email: Email
    shirtNumber: Int
}

input UserInput {
    firstName: String!
    lastName: String!
    birthDate: DateTime!
    gender: Gender!
    email: Email!
    shirtNumber: Int!
}

enum Gender {
    MALE
    FEMALE
}

extend type Query {
    users: [User!]
    currentUser: User!
}

extend type Mutation {
    activateUser(user: UserInput!): ID!
}
